## Agenda for 3/23

- Generating correct Verus syntax. We tried a few things (but none of them work super well)
  - we fixed it using few shot examples (5 right now), but still failing verification often
  - using a more powerful (reasoning?) model -> what model we should pick
  - pointing the model to correct syntax
  - giving the model several tries to "fix" its mistakes -> does this still make sense to do? maybe not
- In general, we have this tension between picking a model that might make mistakes on test cases, but can still do something reasonable for verification
- We are going to start with Rejection Fine Tuning (make sure we have the same definition of this?) instead of RL
- We might have to look at benchmarks beyond Human Eval as well (it may be easier to find training data, but not sure what the final benchmarks should be?)
    - double check understanding that training data does not have to have Verus specifications
- Try to understand how the proposer is trained in PSV
    - for our project, double check that we can use a different model (different than the one we are training) to generate test cases/specifications