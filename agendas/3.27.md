# Things to do 

1. Change the experimental pipeline so that the LLM does not have access to test cases (besides those in the NL prompt). To get some reward hacking behavior, you may have to instruct the LLM to reward hack, etc.
   - Amar

2. Fix syntax issues when Claude goes from unverified Rust to verified Verus
    - might have to play whack a mole
    - if that doesnt work, we might have to make passing Verus syntax a requirement and part of the reward (If we do this, then we could also try few-shot prompting)
    - Elanor

3. Think about if there is a better way to prevent Claude from producing non-proof annotations when going to Verus.
  -  tell Claude that the thing it gets back has to pass python script
  -  and if claude output fails python script, then make it to multiple rounds of edits
  - play whack a mole
  - Amar 

4. We need to come up with a fixed set of target names. This can be done in one pass, using Claude and then checked by a person. Basically you would add an extra field to `data/processed/human_eval_verus.jsonl`
    - Elanor

5. Human eval 129 to Opus -> Elanor

6. Looking into more ways to get compute -> Amar + Elanor